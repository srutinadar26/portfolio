Add your resume as /public/resume.pdf so the "Download Resume" button works.
Add an optional /public/textures/grain.png (subtle paper texture) if you want
to swap the CSS-noise grain for a real scanned texture.
